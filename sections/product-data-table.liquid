{{ 'product-data-table.css' | asset_url | stylesheet_tag }}

{% assign heading = product.metafields.custom.product_data_table_heading %}
{% assign table = product.metafields.custom.product_data_table %}
{% assign lower_text = product.metafields.custom.product_data_table_lower_text %}

{% if heading != blank or table != blank or lower_text != blank %}
  <div class="section section--padded product-data-table">
    <div class="container">

      {% if heading != blank %}
        <h2 class="h4 section__heading text-center">
          {{ heading }}
        </h2>
      {% endif %}

      {% if table != blank %}

        <p class="product-data-table__mobile-note">
          Swipe to view full table →
        </p>

        <div class="product-data-table__wrapper">
          {{ table.value | raw }}
        </div>
      {% endif %}

      {% if lower_text != blank %}
        <div class="product-data-table__lower-text">
          {{ lower_text | metafield_tag }}
        </div>
      {% endif %}

    </div>
  </div>
{% endif %}

{% schema %}
{
  "name": "Product Data Table",
  "tag": "section",
  "class": "section",
  "presets": [
    {
      "name": "Product Data Table"
    }
  ]
}
{% endschema %}